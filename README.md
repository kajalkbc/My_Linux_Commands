### Linux ssh command is used to create a remote connection through the ssh protocol
~~~
ssh user_name@host (IP/Domain_name)
~~~
### The whoami command allows linux users to see the currently logged-in user.
~~~
whoami
~~~
### The ls command designed to list the names and features of files and directories.
~~~
ls
~~~
### The ls-l command displays the contents of the current directory in a long listing format
~~~
ls -l
~~~
### The pwd command is used to display the location of the current working directory.
```
pwd
```
### The cd command is used to change the current directory.
~~~
cd
~~~
### The touch command is used to create empty files. We can create multiple empty files by executing it once.
~~~
touch
~~~
### Vim is a text editor. Vim editor is more helpful in editing different kinds of plain text. This command is more used in editing programs.
~~~
vim
~~~
### Just like vim ,nano is also atext editor 
~~~ 
nano
~~~
### The cat command is a multi-purpose utility in the Linux system. It can be used to create a file, display content of the file, copy the content of one file to another file, and more.
~~~
cat
~~~
### shred is a command on Unix-like operating systems that can be used to securely delete files and devices so that it is extremely difficult to recover them
~~~
shred
~~~
### echo is a built-in Linux command that is used to display the text passed in as an argument. It is one of the basic Linux commands used in shell scripting and Bash files to display output status text at the command line.
~~~
echo
~~~
### The mkdir command is used to create a new directory under any directory.
~~~
mkdir
~~~
### The rmdir command is used to delete a directory.
~~~
rmdir
~~~
### The cp command is used to copy a file or directory.
~~~
cp
~~~
### The mv command is used to move a file or a directory form one location to another location.
~~~
mv
~~~
### The ln command is used to create links to files or directories.
~~~
ln
~~~
### The clear command is used to clear the terminal screen
~~~
clear
~~~ 
### The useradd command is used to add or remove a user on a Linux server.
~~~
useradd
~~~
### The Linux sudo command stands for Super User Do. Generally, it is applied as a prefix of a few commands that superuser is allowed to execute.
~~~
sudo
~~~
### adduser command in Linux is used to add a new user to your current Linux machine. This command allows you to modify the configurations of the user which is to be created. It is similar to the useradd command in Linux. The adduser command is much interactive as compared to useradd command.
~~~ 
adduser
~~~ 
### The su command provides administrative access to another user. In other words, it allows access of the Linux shell to another user.
~~~
su 
~~~
### Linux exit command is used to exit from the current shell. It takes a parameter as a number and exits the shell with a return of status number.
~~~
exit
~~~
### passwd command in Linux is used to change the user account passwords. The root user reserves the privilege to change the password for any user on the system,while a normal user can only change the account password for his or her own account.
~~~ 
passwd
~~~

### The head command is used to display the content of a file. It displays the first 10 lines of a file.
~~~
head
~~~
### The tail command is similar to the head command. The difference between both commands is that it displays the last ten lines of the file content. It is useful for reading the error message.
~~~
tail
~~~
### The find command in UNIX is a command line utility for walking a file hierarchy. It can be used to find files and directories and perform subsequent operations on them.
~~~
find
~~~
### Linux chmod command is used to change the access permissions of files and directories. It stands for change mode. It can not change the permission of symbolic links. Even, it ignores the symbolic links come across recursive directory traversal.
~~~
chmod
~~~
### chown command is used to change the file Owner or group. Whenever you want to change ownership you can use chown command. 
~~~ 
chown
~~~
### The ifconfig command is used to configure the kernel-resident network interface.It is used at the boot time to set up the interfaces as necessary.
ifconfig

### Linux ip command is an updated version of the ipconfig command. It is used to assign an IP address, initialize an interface, disable an interface.
~~~ 
ip address  or
ip a        or
ip addr     or
~~~
### The grep is the most powerful and used filter in a Linux system. The 'grep' stands for "global regular expression print." It is useful for searching the content from a file. Generally, it is used with the pipe.

~~~
grep
~~~
### The awk command is used for text processing in Linux The Awk is a powerful scripting language used for text scripting.
~~~
awk
~~~

### The ping command is used to check the connectivity between two nodes, that is whether the server is connected. It is a short form of "Packet Internet Groper.
~~~
ping
~~~

### The cal command is used to display the current month's calendar with the current date highlighted.
~~~
cal
~~~
### The free command which displays the total amount of free space available along with the amount of memory used and swap memory in the system, and also the buffers used by the kernel.
~~~
free
~~~
### The df command is used to display the disk space used in the file system. It displays the output as in the number of used blocks, available blocks, and the mounted directory.
~~~
df
~~~
### The ps command enables you to check the status of active processes on a system, as well as display technical information about the processes
~~~
ps
~~~
### The Top command is used to show the Linux processes. It provides a dynamic real-time view of the running system. Usually, this command shows the summary information of the system and the list of processes or threads which are currently managed by the Linux Kernel
~~~
top
~~~
### htop command in Linux system is a command line utility that allows the user to interactively monitor the system’s vital resources or server’s processes in real time. 
~~~ 
htop
~~~
### kill command in Linux (located in /bin/kill), is a built-in command which is used to terminate processes manually. kill command sends a signal to a process which terminates the process
~~~ 
kill 
~~~

### history command is used to view the previously executed command
~~~
history
~~~
### Reboot command in Linux is used for rebooting or restarting the system
~~~
reboot
~~~
### The shutdown command in Linux is used to shutdown the system in a safe way. You can shutdown the machine immediately, or schedule a shutdown using 24 hour format
~~~
shutdown
~~~
